# 前端字段名更新提示词

## 📋 字段名变更说明

后端 API 接口 `/api/finished/link/monitor/list` 的返回数据中，以下字段名已更新：

### 变更内容

| 旧字段名 | 新字段名 |
|---------|---------|
| `visitorsVolatility` | `visitorsVolatilityBaseline` |
| `adCostVolatility` | `adCostVolatilityBaseline` |
| `salesVolatility` | `salesVolatilityBaseline` |

### 数据结构

字段类型和内容保持不变，仅字段名更改：

```typescript
{
  visitorsVolatilityBaseline: Array<{
    window: number;
    direction: '+' | '-';
    strength: number;
    level: '极小' | '轻微' | '一般' | '明显' | '剧烈';
  }>;
  adCostVolatilityBaseline: Array<{...}>;
  salesVolatilityBaseline: Array<{...}>;
}
```

### 需要更新的地方

1. 接口响应类型定义
2. 所有使用这些字段的组件和页面
3. 变量名和属性访问

### 说明

新字段名 `*VolatilityBaseline` 更准确地表达了"短期波动相对长期基准指标"的计算概念。

